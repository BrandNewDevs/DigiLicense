# Keep container-local commands working, then reject every plaintext TCP
# connection before allowing TLS-protected PostgreSQL connections.
local     all             all                                     trust
hostnossl all             all             all                     reject
hostssl   all             all             all                     scram-sha-256
