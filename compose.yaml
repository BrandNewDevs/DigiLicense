services:
  web:
    build:
      context: .
    ports:
      - "3000:3000"
    env_file:
      - apps/web/.env
    volumes:
      - .:/workspace
      - node_modules:/workspace/node_modules
    command: pnpm --filter web exec vite dev --host 0.0.0.0 --port 3000


volumes:
  node_modules:
